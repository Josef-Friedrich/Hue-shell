#! /bin/sh

. /etc/hue-shell/hue-shell.conf

case "$1" in

	-h|--help)
		_hue_usage
		;;

	-m|--master-pid)
		MASTER_PID=$2
		shift 2
		;;
esac

echo $$ > $HOME/.config/hue-shell/daemon.pid
eval "$DEFAULT_SCENE"

# vim: set ts=8 sw=8 sts=8 et :
# sublime: tab_size 8;
